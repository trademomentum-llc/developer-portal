apiVersion: score.dev/v1b1
metadata:
  name: with-secret
containers:
  web:
    image: registry.example.com/ws:1
    variables:
      TOKEN: "${resources.example.password}"
resources:
  example:
    type: secret
    metadata:
      name: example-secret
