# platform-addons

Flux-watched repository. Contents under `clusters/default/` are reconciled
every minute into the cluster. Edit via PR only.

Portal: http://localhost:3001/catalog/default/resource/platform-addons
