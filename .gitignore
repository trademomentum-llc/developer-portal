# Compiled binaries
plugins/rr-policy-guards/bin/

# OpenTofu / Terraform local state and provider cache
**/.terraform/
*.tfstate
*.tfstate.*
.terraform.tfstate.lock.info

# Backstage
backstage/node_modules/
backstage/.yarn/cache/
backstage/.yarn/install-state.gz
backstage/dist/
backstage/dist-types/
backstage/.cache/

# OS
.DS_Store

# Secrets (never commit)
*.env
*.env.*
